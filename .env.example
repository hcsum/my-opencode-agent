# Telegram bridge only.
TELEGRAM_BOT_TOKEN=123456:replace-me
TELEGRAM_ALLOWED_CHAT_ID=123456789
OPENCODE_BASE_URL=http://127.0.0.1:4096
# Optional if your OpenCode server uses basic auth.
OPENCODE_SERVER_USERNAME=opencode
OPENCODE_SERVER_PASSWORD=
# Optional OpenCode serve runtime (used by npm run serve:opencode).
OPENCODE_SERVE_PORT=4096
# Optional shared proxy for OpenCode serve + Gmail bridge.
HTTPS_PROXY=http://127.0.0.1:7890

# Used by the local seo MCP server when needed.
CAPSOLVER_API_KEY=

# Optional Telegram bridge overrides.
TELEGRAM_SESSION_TITLE="OpenCode Agent"
STATE_FILE=.data/state.json

# Optional Google Drive notes backup target folder.
GDRIVE_NOTES_BACKUP_FOLDER_ID=

# Optional Gmail bridge.
# Gmail OAuth credentials must be in ~/.gmail-mcp/ (gcp-oauth.keys.json + credentials.json).
GMAIL_TO=sumtsui+agent@gmail.com
GMAIL_POLL_INTERVAL_MS=60000

# if use codex client, set to true to enable network access for codex.
CODEX_NETWORK_ACCESS=true
CODEX_SANDBOX_MODE=workspace-write

